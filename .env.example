# DrivenMetrics MCP Environment Variables Template
# Copy this file to .env and fill in your actual values

# ==========================================
# REQUIRED - Server won't work without these
# ==========================================

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
# IMPORTANT: Use the SERVICE ROLE key from Supabase Dashboard > Settings > API
# NOT the anon key! The service role key is much longer and bypasses RLS
SUPABASE_KEY=eyJ...your-service-role-key-here...

# Server Configuration
PORT=3000

# ==========================================
# OPTIONAL - Enhanced functionality
# ==========================================

# Facebook OAuth Configuration (for Facebook Ads API)
FB_APP_ID=your-facebook-app-id
FB_APP_SECRET=your-facebook-app-secret

# Facebook Ad Library Configuration
DEFAULT_AD_COUNTRY=IT  # Default country for ad searches (e.g., IT, US, GB, ALL)

# Anthropic Claude API Configuration
ANTHROPIC_API_KEY=sk-ant-api03-your-api-key-here

# n8n Webhook Configuration (optional - for legacy chat integration)
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id/chat

# Session Configuration
SESSION_SECRET=your-random-session-secret-here
SESSION_MAX_AGE=86400000  # 24 hours in milliseconds

# OAuth Configuration
OAUTH_CLIENT_ID=your-oauth-client-id
OAUTH_REDIRECT_URI=http://localhost:3000/callback

# ==========================================
# DEVELOPMENT ONLY
# ==========================================

# Debug mode
NODE_ENV=development
DEBUG=true

# ==========================================
# NOTES
# ==========================================
# 
# 1. Never commit the actual .env file to git
# 2. Generate strong random secrets for production
# 3. Use different values for dev/staging/production
# 4. Rotate keys regularly for security
# 5. The Supabase SERVICE_ROLE key is critical - without it, the app won't work
#
# To generate a random secret:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#